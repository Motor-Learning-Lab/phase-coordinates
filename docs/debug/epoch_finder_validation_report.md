# Epoch-finder validation report

Generated by `docs/debug/scripts/validate_epoch_finders.py`. Raw output:
`docs/debug/epoch_finder_metrics.csv`, `docs/debug/epoch_finder_top_candidates.csv`,
`docs/debug/epoch_finder_winding_checks.csv`.

## Round 4: correctness/contract fixes (post-review correction round)

A focused review round fixed several correctness and contract issues
elsewhere in the deterministic layer (quadratic `sample_start`/`sample_stop`
lookup, the `1 - PC3` planarity bug and dimensionality contract, Bayesian
output-contract parity, non-deterministic selection tie-breaks, overbroad
exception handling, and endpoint-completion ordering/bounds -- see the
round's commit message and PR description for the full list; this section
covers only what's relevant to *this* validation suite). Rerunning it
unchanged after the fixes:

**Winding values are now exact, not approximate** (Fix 4: the winding
calculation now closes the gap from the last recorded sample to the
cycle's own end boundary, using an interpolated anchor, instead of
systematically undercounting by ~1/n of a revolution):

| Check | Before this round | After |
|---|---|---|
| true_period (1x) | 0.990 | **1.000** |
| multi_lap_2x | 1.990 | **2.000** |
| multi_lap_3x | 2.990 | **3.000** |
| fractional_lap_0.5x | 0.673 | **0.680** (matches the analytic continuous-limit value exactly now, see below) |
| mild_phase_warp | 0.988 | **1.000** |
| traversal_reversal | 0.990 | **1.000** |

All validity *decisions* (accept/reject) are unchanged — every case was
already on the correct side of the default `[0.75, 1.25]` range before this
fix; the fix makes the diagnostic values themselves trustworthy at low
sample counts, where the old undercounting was large enough to flip a
decision (see `tests/test_pipeline.py::test_winding_low_samples_per_cycle_still_reads_one_revolution`,
which specifically covers the 3-samples-per-cycle case the original report
flagged as a real risk: `2/3 ≈ 0.667`, below the validity floor, purely
from the missing segment).

**Selected candidates are unchanged in cycle count on all 5 scenarios**,
confirming no regression from either the planarity fix (algebraically a
no-op for this suite -- every scenario already uses exactly 3 dimensions,
so `evr[0]+evr[1] == 1-evr[2]` held already) or the deterministic
tie-break (Fix 5). One metric did improve, though: `phase_offset_start`'s
`geometric_score` row went from `median_tau_error_samples` 40.0 → **19.9**
and `max_tau_error_samples` 43.0 → **22.9** -- the deterministic
compound ordering (prefer smaller offset among remaining ties, rather than
whichever the search happened to visit first) landed on a better-aligned
offset among the near-tied winners.

**Runtime**: two consecutive full-suite runs measured ~40.0s and ~40.5s,
up from the ~33.5s measured right after Fix 4 alone (before this round's
Fixes 5-7). None of Fixes 5-7 add per-candidate cost that should plausibly
account for a ~20% increase on their own (Fix 5 swaps a single-int sort key
for a 4-tuple one; Fix 6 adds a handful of one-time upfront checks; Fix 7
touches `bayesian.py`, not this search path at all) -- reported honestly
without a confirmed root cause; the most likely explanation is ordinary
system-level variance between measurement sessions rather than an
algorithmic regression, but this was not root-caused further.

This branch does **not** touch the Bayesian model. It only compares two
deterministic ways of turning a 3-D trajectory into cycle boundaries:

- **peak_seed** — `dominant_reference_signal` → `estimate_dominant_period` →
  `seed_boundary_indices` → `epochs_from_boundary_indices` (the path
  currently used to seed Bayesian Layer 1).
- **geometric_score** — `period_candidates_from_periodogram` /
  `period_candidates_from_autocorrelation` → `expand_period_harmonics` →
  `find_epochs_by_geometric_score` (the new candidate replacement).
- **phase_ground_truth** — `identify_cycles_from_phase` on the exact
  synthetic phase, included only as an upper-bound sanity reference, not
  part of the A-vs-B comparison (see caveat below).

## Round 3: winding-based candidate validity check

Round 2's `score_tolerance` fix resolved the demonstrated `phase_offset_start`
failure, but as a *tie-break heuristic*: it only works because a correctly-
scored true-period candidate happened to also be in the search and score
within noise of the wrong one. This round adds a direct, standalone test of
whether a candidate cycle represents one traversal of the loop at all,
independent of whether any competing candidate exists to tie-break against.

### Winding calculation

For each cycle, using the in-plane (PC1, PC2) coordinates of the per-cycle
PCA already fit for `planarity` (reused via `pca.transform`, not refit — an
O(n) projection):

```python
scores = pca.transform(X_k - center_k)
u, v = scores[:, 0], scores[:, 1]
cross = u[:-1] * v[1:] - v[:-1] * u[1:]
dot   = u[:-1] * u[1:] + v[:-1] * v[1:]
delta_theta = np.arctan2(cross, dot)          # signed angle per consecutive pair
winding = np.sum(delta_theta) / (2 * np.pi)   # signed revolutions
```

**Degeneracy handling.** Samples whose in-plane radius `hypot(u, v)` is below
5% of the cycle's own median radius are excluded pairwise (their angle is
noise-dominated, not meaningful) — a transition needs *both* endpoints
non-degenerate. If fewer than 50% of a cycle's consecutive-sample
transitions end up well-defined, the winding is left undefined (`NaN`)
rather than computed from an unreliable minority; a `NaN`-winding cycle
never counts as single-lap when aggregating (see below), so a
mostly-degenerate cycle cannot silently look valid. Both thresholds are
`scoring.py` module constants (`_WINDING_DEGENERATE_RADIUS_FRAC`,
`_WINDING_MIN_VALID_TRANSITION_FRACTION`) — internal numerical-robustness
details of *computing* the diagnostic, not user-facing validity settings
(those are the range/fraction below, which *are* keyword arguments).

`score_epoch_geometry` reports, per candidate: `winding_median_abs`,
`winding_min_abs`, `winding_max_abs`, and `fraction_single_lap_cycles`
(fraction of *all* cycles — including degenerate ones, which never count —
with `abs(winding)` inside a caller-supplied `[winding_valid_min,
winding_valid_max]` range). It does not decide validity; it only measures.

### Validity rule and tolerance

`find_epochs_by_geometric_score` gains:

```python
winding_valid_min: float = 0.75
winding_valid_max: float = 1.25
winding_min_fraction: float = 0.8
require_winding_valid: bool = True
```

A candidate is **winding-valid** iff `fraction_single_lap_cycles >=
winding_min_fraction`. Winding-invalid candidates are excluded from
selection entirely — a hard filter, same tier as the existing
`epochs.n_cycles < 2` exclusion — applied *before* `score_tolerance`, not
folded into `total_score`.

**Why a fraction-of-cycles rule, not "every cycle" or "any cycle".**
Requiring every nondegenerate cycle to pass is fragile to one noisy cycle
rejecting an otherwise-correct candidate. Requiring only one cycle to pass
would fail to reject genuine period errors, since those are systematic —
every cycle of a 2x/3x/0.5x-period candidate shows the same wrong winding,
not just one. A fraction threshold (`0.8`) tolerates the former (up to ~20%
of cycles can be noisy/degenerate) while still reliably rejecting the
latter (a systematic error fails essentially 100% of cycles, far below any
reasonable fraction threshold).

### Sensitivity to tolerance choices

Checked directly against known period errors (`epoch_finder_winding_checks.csv`,
using the `clean_complete_cycles` trajectory, true period 1.0 s):

| Candidate | Measured `abs(winding)` | Falls in `[0.75, 1.25]`? |
|---|---|---|
| true period (1x) | 0.990 | yes — valid |
| 2x true period | 1.990 | no — rejected |
| 3x true period | 2.990 | no — rejected |
| 0.5x true period | 0.673 | no — rejected |

The measured values sit far from the `[0.75, 1.25]` boundary in every
direction (nearest miss is 0.5x at 0.673, still 0.077 outside the 0.75
boundary) — the default range is not on a knife-edge for these cases.
Widening it to e.g. `[0.6, 1.4]` would still correctly reject all three
error cases here; narrowing much past `[0.85, 1.15]` starts to risk
false-rejecting genuine noisy-but-correct cycles (the `noisy_complete_cycles`
scenario's individual-cycle windings were not all exactly 1.0 — see below).
Not exhaustively swept beyond these checks; see "Remaining limitations."

**Important, non-obvious finding: the fractional-lap winding value is
biased, not simply the period ratio.** A 0.5x-period candidate does *not*
measure ≈0.5 — it measures ≈0.673 (confirmed analytically: a continuous
half-arc's own centroid sits `2/π ≈ 0.637` from the true circle center
along the symmetry axis, not at the center, because the per-cycle center is
the *sample mean of that cycle's own points* — reused from the existing
planarity computation, per the task's explicit instruction to reuse rather
than refit). Averaging over a **partial** revolution doesn't cancel to the
true center the way averaging over one or more **complete** revolutions
does, so the angle measured around that off-center mean is inflated above
the naive period-fraction guess. This doesn't affect the validity decision
here (0.673 is still well outside `[0.75, 1.25]`), but it means
`winding_valid_min` should not be lowered much below its default without
re-checking against this bias — a value like 0.65 would risk accepting a
genuine half-period error. Documented in code
(`docs/debug/scripts/validate_epoch_finders.py`, the `fractional_lap_0.5x`
check) and in `tests/test_pipeline.py`, not hidden or tuned around.

### Before vs. after (Round 2 → Round 3, all 5 scenarios)

| Scenario | Method | n_cycles Round 2 → Round 3 | Winner unchanged? |
|---|---|---|---|
| clean_complete_cycles | geometric_score | 6 → 6 | yes |
| noisy_complete_cycles | geometric_score | 5 → 5 | yes |
| phase_offset_start | geometric_score | 6 → 6 | yes |
| mild_phase_warp | geometric_score | 5 → 5 | yes |
| harmonic_ambiguity_candidate | geometric_score | 6 → 6 | yes |

**No regressions and no additional fixes on this suite** — the winding
filter accepted exactly the same winners `score_tolerance` alone already
found. This is expected, not a null result: Round 2 already fixed the one
demonstrated failure this suite exercises (`phase_offset_start`'s
period-multiple ambiguity), and the winding filter is a *more direct*
mechanism for the same underlying problem, not a different one — agreeing
with the prior fix here confirms consistency, not that the new mechanism
was untested. Its value over Round 2's tie-break is in cases Round 2 can't
reach at all (see "Whether score_tolerance still affects any winner").

### Explicit multi-lap / fractional-lap / reversal / warp checks

All 7 direct checks pass (`epoch_finder_winding_checks.csv`):

| Check | Measured | Expected range | Valid? | Passed |
|---|---|---|---|---|
| true_period (1x) | 0.990 | (0.9, 1.1) | True | yes |
| multi_lap_2x | 1.990 | (1.8, 2.2) | False (correctly rejected) | yes |
| multi_lap_3x | 2.990 | (2.8, 3.2) | False (correctly rejected) | yes |
| fractional_lap_0.5x | 0.673 | (0.6, 0.75) — see bias note above | False (correctly rejected) | yes |
| mild_phase_warp | 0.988 | (0.9, 1.1) | True | yes |
| traversal_reversal | 0.990 | (0.9, 1.1) | True | yes |
| traversal_reversal_sign | signed winding flips sign | n/a | True | yes |

**Mild phase warp remains accepted**: winding 0.988, within the noise band
already seen for the unwarped true-period case (0.990) — the quarter-cycle
non-uniform angular velocity (`phase += 0.3*sin(2*pi*t/T)`) does not measurably
perturb the winding diagnostic. **Traversal reversal**: confirmed the signed
winding flips sign under time-reversed sample order while `abs(winding)`
stays valid — verified with a construction where forward and reversed calls
share the *identical* per-cycle PCA fit (fit is order-invariant, depending
only on the point set), which was necessary to avoid a real confound:
independently refitting PCA on a forward vs. reversed *multi-cycle* array
can pick oppositely-signed axes for reasons unrelated to traversal direction
(a symmetric circle has no intrinsic handedness), which nearly produced a
false test failure — documented in `tests/test_pipeline.py`.

**Also checked under `noisy_complete_cycles`** (already part of the main
suite, std=0.03 noise): `geometric_score`'s winner still shows
`fraction_single_lap_cycles` fully satisfied (winner unchanged at 5/6
cycles, `winding_median_abs` within the valid range) — noise at this level
doesn't push any winning cycle's winding out of range.

### Whether `score_tolerance` still affects any winner

**Yes — retained, not redundant.** Comparing `score_tolerance=0.0` (strict
argmax) against `score_tolerance=0.001` (current default), both *with*
winding filtering applied, on all 5 scenarios:

| Scenario | winding-valid candidates (of scored) | n_cycles @ tol=0 | n_cycles @ tol=0.001 |
|---|---|---|---|
| clean_complete_cycles | 64 / 258 | 5 | **6** |
| noisy_complete_cycles | 128 / 1025 | 5 | 5 |
| phase_offset_start | 128 / 1228 | 6 | 6 |
| mild_phase_warp | 64 / 706 | 5 | 5 |
| harmonic_ambiguity_candidate | 146 / 962 | 5 | **6** |

In 2 of 5 scenarios, `score_tolerance` still changes the winner *after*
winding filtering — among winding-valid candidates, there can still be a
near-tied lower-coverage one that a strict argmax would pick over a
marginally-lower-scoring higher-coverage one. The winding filter answers "is
this candidate a genuine single lap"; `score_tolerance` still answers "among
several genuine single-lap candidates that are statistically tied on
geometric quality, which do we prefer" — confirmed to be a real, distinct
question empirically, not just in principle. Per the task's instructions,
`score_tolerance` is retained.

### Runtime impact

Full 5-scenario suite (`run_all()`, dominated by the geometric-score search
grid — thousands of (period, offset) candidates across the 5 scenarios):

| | Time |
|---|---|
| Before winding check (Round 2) | 21.8 s |
| After winding check (Round 3) | 33.5 s |
| Delta | +11.7 s (+54%) |

The new `run_winding_diagnostic_checks()` function (7 direct adversarial
checks) itself takes ~0.01 s — negligible; essentially all of the increase
is the winding computation running once per cycle for every scored
candidate in the search. This roughly doubles the per-cycle cost inside
`score_epoch_geometry` (one extra O(n) `pca.transform` + cross/dot sum on
top of the existing O(n) `pca.fit`), consistent with the ~50% increase
observed — a proportional, expected cost of an O(n)-per-cycle addition, not
a complexity blowup. Not a concern at this suite's scale (tens of seconds);
worth reassessing if period-search grids or the number of scenarios grow
substantially.

### Remaining limitations

- Tolerance sensitivity was checked at the specific measured values above,
  not swept systematically across a grid of `winding_valid_min/max` /
  `winding_min_fraction` combinations — the margins found are comfortable
  but not exhaustively characterized.
- The fractional-lap winding bias (partial-arc centroid offset) means the
  *numeric* winding value for a fractional-period error is not simply
  "period ratio" — correct interpretation requires knowing this, and it
  hasn't been checked how the bias behaves for fractions other than 0.5x
  (e.g. 0.33x, 0.75x) or for non-circular (e.g. eccentric) trajectories.
  Validation on all synthetic data uses a single tilted circle shape.
  Documented, not hidden — flagged as a known way the diagnostic's raw
  values can be misread, not something fixed here per the task's
  instructions not to redesign the score.
- The winding filter and `score_tolerance` were validated together, not
  independently ablated across a wider set of adversarial cases beyond the
  ones this round constructed.

## Round 2: fixes for the two demonstrated failures

Round 1 (below) found two concrete deterministic failures. Both are now
fixed; the same, unmodified validation suite was rerun to confirm.

### Fix 1 — coverage-aware candidate *selection* in `find_epochs_by_geometric_score`

**Not** implemented as a coverage term inside `total_score`. Investigation
first (see "Why not gate on raw coverage" below) ruled out the obvious
"restrict to candidates near the max coverage" approach as actively unsafe,
then landed on a different, narrower mechanism:

Restrict attention to candidates within an **absolute `total_score`
tolerance** of the best score achieved by any candidate (`score_tolerance`,
default `0.001`), then, among only those near-tied candidates, prefer the
one with the most complete cycles (`n_cycles`). `total_score` itself is
completely unchanged — this only changes which of several *already
similarly-scored* candidates is returned.

**Why this and not a raw coverage gate.** The obvious first idea — keep
only candidates within some tolerance of the maximum `n_cycles` (or
`coverage_duration_fraction`) achieved by any candidate, then pick the best
`total_score` among those — was checked against the full (not just top-10)
candidate table before implementing anything, and rejected:

- `coverage_duration_fraction` and `fraction_samples_assigned` turned out to
  be **useless for this** in the exact case that motivated the task: in
  `phase_offset_start` the winning bad candidate (2 cycles of 2.99 s) and
  the correct candidate (6 cycles of 0.99 s) have nearly *identical*
  duration-based coverage (0.919 vs. 0.912) — both configurations happen to
  span almost the same fraction of the recording, so a duration-based
  coverage gate would not have distinguished them at all.
- Gating on raw `n_cycles` instead is worse, not better: the full candidate
  table for `clean_complete_cycles` contains candidates with **11, 12, 19,
  20, 44, 45, 47, 48, 57, 90, ...** cycles (short-period candidates from
  `expand_period_harmonics`'s 0.5x multiplier and `period_search`'s
  `min_period` floor), all scoring far worse (~0.62–0.89) than the correct
  6-cycle candidate (~0.9996) but with much higher raw cycle counts. A
  coverage-first filter with any small tolerance would have **discarded the
  correct answer in favor of a spurious high-count one** — the opposite of
  the intended fix.

The actual mechanism behind the `phase_offset_start` failure, once the full
candidate table was inspected by `n_cycles`, turned out to be a different
and more fundamental ambiguity than "short segment, good score": **an
exact integer multiple of the true period retraces the same physical loop
multiple times per detected "cycle," which is geometrically indistinguishable
from the true period** (same planarity, same anchor placement — it's
literally the same positions revisited). `total_score` is correctly
measuring geometric quality; it just can't distinguish "1 lap" from "3 laps
end-to-end" on its own, because they *are* geometrically identical.
Gating on score first (keep only the near-tied top of the score
distribution — genuinely equivalent-quality candidates) and breaking that
tie by preferring more independent repetitions is what actually targets
this ambiguity, and it's naturally safe against the spurious-short-period
trap: those candidates score far below the tolerance band and are excluded
before coverage is even considered.

`score_tolerance=0.001` was chosen empirically by inspecting the max score
achieved at each `n_cycles` value across all 5 scenarios: genuinely
ambiguous (same-loop-retraced) candidates cluster within ~0.0002 of the
best score, while the next tier of genuinely-different, worse candidates is
always ≥0.004 away. `0.001` sits cleanly in that gap in every scenario
tested; not aggressively tuned beyond that.

### Fix 2 — `seed_boundary_indices` endpoint completion

`scipy.signal.find_peaks` cannot report a peak at index 0 or the last
index of an array (a peak requires a neighbor on both sides), so a genuine
cycle boundary that coincides with the start or end of a recording is
otherwise always missed.

Implemented a bounded, evidence-based completion step, **not** an
unconditional "treat the endpoints as boundaries" rule (explicitly
forbidden by the task): after the normal peak search, look for one
additional peak just before the first detected peak and just after the
last one, at the position the estimated period `T0` predicts. The
candidate location is accepted only if **both** hold:

1. It falls within the recorded data at all (period-based extrapolation
   from the nearest known peak stays in-bounds) — uses expected peak
   spacing / estimated period / neighboring peak locations.
2. The reference signal actually looks like a peak there — an interior
   local max in a small window around the predicted position, or (at the
   array edge itself) the signal is monotonically moving away from that
   edge in the direction a peak's far side would — uses the reference
   signal's own endpoint behavior.

This declines to add anything when there's no room for a full extra cycle,
or the data at the predicted location doesn't actually look like a peak —
verified directly: `tests/test_pipeline.py::test_seed_boundary_indices_completes_genuine_edge_peaks`
constructs a signal with real peaks at both array edges and confirms both
are recovered; `test_seed_boundary_indices_no_completion_without_room`
confirms it's a no-op when there isn't a genuine edge peak to find (the
same construction as this suite's `clean_complete_cycles` scenario).

### Before vs. after

| Scenario | Method | n_cycles before → after | period_error before → after |
|---|---|---|---|
| clean_complete_cycles | geometric_score | 5 → **6** | +0.0017 → +0.0017 |
| clean_complete_cycles | peak_seed | 5 → 5 (unchanged) | +0.0017 → +0.0017 |
| noisy_complete_cycles | geometric_score | 5 → 5 (unchanged) | +0.0017 → +0.0017 |
| noisy_complete_cycles | peak_seed | 5 → 5 (unchanged) | +0.0017 → +0.0017 |
| **phase_offset_start** | **geometric_score** | **2 → 6** | **+1.9900 → −0.0100** |
| phase_offset_start | peak_seed | 6 → 6 (unchanged) | −0.0700 → −0.0700 |
| mild_phase_warp | geometric_score | 5 → 5 (unchanged) | +0.0017 → +0.0017 |
| mild_phase_warp | peak_seed | 5 → 5 (unchanged) | +0.0017 → +0.0017 |
| harmonic_ambiguity_candidate | geometric_score | 5 → **6** | +0.0017 → +0.0017 |
| harmonic_ambiguity_candidate | peak_seed | 5 → 5 (unchanged) | +0.0017 → +0.0017 |

`phase_ground_truth` unaffected everywhere, as expected (no changes made to
`identify_cycles_from_phase`). Every `total_score`, `planarity`,
`anchor_norm`, `quarter_anchor_orth_ratio` value in the rerun stayed within
noise of its previous value — no regressions anywhere in the suite. Full
numbers in `epoch_finder_metrics.csv` (git history has the pre-fix version
for exact diffing).

**1. Is the phase-offset-start failure resolved?** Yes. `geometric_score`
now finds 6/6 cycles at period 0.99 s (period_error −0.01, down from
+1.99), matching `peak_seed` and `phase_ground_truth`. τ-error also dropped
sharply (61.0/138.0 → 40.0/43.0 median/max samples).

**2. Does peak seeding now correctly identify endpoint boundaries?** The
fix is implemented and verified correct on a direct unit test (recovers a
genuine edge peak when one exists), but it is a **no-op across this
specific validation suite** — see "A finding, not a fix, for peak_seed"
below. `peak_seed`'s 5/6 shortfall in `clean/noisy/mild_warp/harmonic`
persists unchanged.

**3. Regressions?** None observed. Every unaffected metric matched its
pre-fix value; the two `n_cycles` improvements (clean, harmonic) were
bonus fixes from the same score-tolerance change that fixed
`phase_offset_start`, not new scenarios or reruns.

**4. Remaining weaknesses:** see "Answers to the stated questions" and
"Failure modes observed" below, updated for the post-fix state.

### A finding, not a fix, for peak_seed

Investigating *why* `seed_boundary_indices`' 5/6 results didn't change
turned up something more precise than Round 1's diagnosis. Round 1's report
attributed the shortfall generally to "`find_peaks` can't place a peak at
an array edge." Checking the actual peak locations and the reference
signal's endpoint behavior shows that's not quite what's happening in
*this* suite: in `clean_complete_cycles`, the reference signal's peaks
land at samples 50, 150, ..., 550 — a full half-period *inside* both
edges, not at them. The true boundaries (samples 0, 100, ..., 600) sit at
a *trough* of this particular reference signal, not a peak, so there is no
edge-adjacent peak for `find_peaks` (or the new completion step) to miss —
the data genuinely doesn't contain one. Extrapolating one period back from
the first peak (50 − 100.17 ≈ −50) or forward from the last (550 + 100.17
≈ 650, past the 601-sample window) lands out of bounds in both directions,
confirmed directly by both the extrapolation arithmetic and the new
completion step declining to add anything.

The real cause is the same phase-anchor mismatch already documented in
Round 1's caveat: `dominant_reference_signal`'s peak sits at a
data-dependent phase (here, exactly half a cycle from this construction's
arbitrary "true phase = 0"), and peak-to-peak cycle counting discards up to
one cycle's worth of data at each edge as a result — not because of a
detection bug, but because there's a genuine half-cycle of unusable data at
each end under that definition. Endpoint completion is still a real, needed
fix in general (confirmed by the unit test recovering a *genuine* edge
peak), it just isn't the dominant limitation this particular suite happens
to exercise. **This is now the clearest remaining weakness in `peak_seed`**
and is flagged, not fixed, per the task's instructions.

## Round 1: initial validation

*(Historical record of the original run and the two failures that led to
Round 2's fixes above.)*

### Summary table

`n_cycles_true` is always 6 in the count-based scenarios and 6 in
`phase_offset_start` too (see Scenarios). Full per-scenario numbers in
`epoch_finder_metrics.csv`; condensed here:

| Scenario | Method | Cycles found / true | Period error (s) | Median / max τ error (samples) |
|---|---|---|---|---|
| clean_complete_cycles | peak_seed | 5 / 6 | +0.0017 | 50.0 / 50.0 |
| clean_complete_cycles | geometric_score | 5 / 6 | +0.0017 | 2.6 / 97.0 |
| clean_complete_cycles | phase_ground_truth | 6 / 6 | 0.0000 | 0.0 / 0.0 |
| noisy_complete_cycles | peak_seed | 5 / 6 | +0.0017 | 38.0 / 62.0 |
| noisy_complete_cycles | geometric_score | 5 / 6 | +0.0017 | 33.4 / 66.3 |
| noisy_complete_cycles | phase_ground_truth | 6 / 6 | 0.0000 | 0.0 / 0.0 |
| phase_offset_start | peak_seed | 6 / 6 | −0.0700 | 12.0 / 12.0 |
| phase_offset_start | **geometric_score** | **2 / 6** | **+1.9900** | 61.0 / 138.0 |
| phase_offset_start | phase_ground_truth | 6 / 6 | 0.0000 | 37.0 / 37.0 (anchoring artifact, see caveat) |
| mild_phase_warp | peak_seed | 5 / 6 | +0.0017 | 50.0 / 53.0 |
| mild_phase_warp | geometric_score | 5 / 6 | +0.0017 | 14.6 / 85.1 |
| mild_phase_warp | phase_ground_truth | 6 / 6 | 0.0000 | 0.0 / 0.0 |
| harmonic_ambiguity_candidate | peak_seed | 5 / 6 | +0.0017 | 50.0 / 51.0 |
| harmonic_ambiguity_candidate | geometric_score | 5 / 6 | +0.0017 | 11.5 / 88.2 |
| harmonic_ambiguity_candidate | phase_ground_truth | 6 / 6 | 0.0000 | 0.0 / 0.0 |

### Caveat: what the τ-error numbers do and don't mean

Two of the methods (`peak_seed`, and `phase_ground_truth` in every scenario
except `phase_offset_start`) do not share the synthetic data's arbitrary
"phase = 0" reference point, so their boundary *offset* is not directly
comparable to `true_tau` even when their boundary *spacing* (period) is
essentially perfect:

- `peak_seed` places boundaries at peaks of `dominant_reference_signal`
  (a PCA projection). For a symmetric circle this peak sits at a
  data-dependent phase, not necessarily "phase = 0" — in `clean_complete_cycles`
  it lands at exactly `period/2` (boundaries at 0.5, 1.5, 2.5, ... instead of
  0, 1, 2, ...). `median_duration_error_samples = 0.0` for that same row —
  i.e. peak_seed tracked cycle-to-cycle spacing essentially perfectly, and
  the large τ-error is entirely a phase-anchor artifact, not a timing error.
- `identify_cycles_from_phase` always anchors cycle 0 to sample 0
  ("first_sample" convention), so in `phase_offset_start` it necessarily
  disagrees with `true_tau` (which starts at the true offset, 0.37 s) by
  construction — the 37-sample error there is expected, not a phase-recovery
  failure.

**Take `period_error` / `median_duration_error_samples` as the fairer
"did this method get the timing right" metric across methods with different
phase anchors. Take τ-error at face value only for `geometric_score`**,
which explicitly searches over phase offset and does try to agree with an
externally meaningful reference point.

### Bug found and fixed during validation

`find_epochs_by_geometric_score` crashed with an uncaught `ValueError` on
the very first scenario. A degenerate period candidate (a harmonic-halved,
low-confidence periodogram peak near `period_search`'s `min_period` floor,
so cycles only ~1 sample long) produced a quarter-cycle anchor time that
fell fractionally outside the recorded data window, which
`interp_X_at_times(bounds_error=True)` correctly refused to extrapolate.
This is a robustness gap in the candidate *search loop*, not the scoring
*rule* — fixed by catching that one exception and skipping the
un-scoreable candidate (`phase_coordinates/scoring.py`), which changes
nothing about how any scoreable candidate is scored.

### Scenarios implemented

1. **clean_complete_cycles** — exact tilted circle, no noise, 6 complete
   1.0 s cycles, `n_time = n_cycles*100 + 1` samples so the last true
   boundary lands on a real recorded sample.
2. **noisy_complete_cycles** — same, + isotropic Gaussian noise (std=0.03).
3. **phase_offset_start** — recording starts 0.37 s before the first true
   boundary (light noise, std=0.01).
4. **mild_phase_warp** — angular velocity varies within each cycle
   (`phase += 0.3*sin(2*pi*t/T)`); the warp term is exactly zero at every
   true boundary by construction, so `true_tau` is unchanged from the clean
   case while within-cycle motion is non-uniform.
5. **harmonic_ambiguity_candidate** — radius modulated at period `T/2`
   (`0.5*cos(2*phase)`) superimposed on the true period-`T` angular motion,
   designed to tempt a spectral estimator into a 0.5x/2x period mistake.

A construction bug was caught and fixed while building these: the first
version defined `true_tau` up to `n_time/fs` (the package's own permissive
half-open convention, which allows a closing boundary with no real sample
there). That demanded recovery of a boundary that literally has no
corresponding data point, which no empirical method (peak detection,
phase-crossing) could ever satisfy. Ground truth now only counts boundaries
with an actual recorded sample at that time.

### phase_offset_start — the original negative result

`geometric_score` found only 2/6 cycles, locking onto a period of
**2.99 s** (period_error +1.99 s) instead of the true 1.0 s. The full
candidate table (not just the top 10) made the mechanism clear:

| period | n_cycles | total_score | source |
|---|---|---|---|
| 2.99 (winner) | 2 | 0.999945 | autocorrelation |
| 0.99 (correct) | 6 | 0.999899 | autocorrelation |

The winning score beat the correct answer by **0.000046** — see Round 2
above for the diagnosis (2.99 ≈ 3× the true period, so the "2-cycle"
candidate is retracing the same true loop 3 times per detected cycle,
which is geometrically indistinguishable from tracing it once) and the fix.

## Answers to the stated questions (current, post-fix)

1. **Does geometric scoring recover the true period on clean complete
   cycles?** Yes, to ~0.2%, and now recovers all 6 cycles (was 5/6).
2. **Does it choose a better phase offset than peak_seed?** In
   `phase_offset_start`, yes, now that the period/coverage ambiguity is
   resolved: geometric_score's period estimate (0.99 s, error −0.01) is
   closer to true than peak_seed's (0.93 s, error −0.07). For the
   symmetric-circle scenarios (clean/noisy/warp/harmonic) this question
   still doesn't have a well-defined answer — a perfectly symmetric shape
   has no geometrically meaningful "true" phase offset for any geometric
   method to recover.
3. **Does it avoid obvious 0.5x/2x harmonic mistakes?** Yes in
   `harmonic_ambiguity_candidate` (always did). The near-3x mistake in
   `phase_offset_start` is now also avoided, via the coverage-tie-break fix
   rather than by improving harmonic rejection specifically — worth
   remembering that the fix targets *exact-multiple* ambiguity generally,
   not this one harmonic case.
4. **Does it discard too much data by choosing a short high-quality
   segment?** No longer, in every scenario this suite exercises. The
   underlying risk (two candidates scoring within noise of each other)
   isn't eliminated in general — `score_tolerance` makes the *selection*
   coverage-aware for candidates that are already near-tied on geometric
   quality; a candidate that's both short *and* has a genuinely, unambiguously
   higher score (not just tied) would still win, correctly.
5. **Does it behave gracefully under mild phase warping?** Yes, unchanged
   from Round 1 — no measurable degradation versus the clean case, before
   or after the fixes.
6. **Which score components are actually informative?** Unchanged from
   Round 1: `planarity` and `quarter_anchor_orth_ratio` never dropped below
   ~0.83 anywhere in this suite, so it doesn't distinguish their individual
   contributions. `candidate_score` (spectral confidence) is still computed
   but never used in selection — the fix implemented here (`score_tolerance`
   + coverage tie-break) does not use it either; it remains an available,
   unused signal for future work.

## Failure modes observed (current, post-fix)

- **peak_seed**: still undercounts by one cycle whenever peak-to-peak
  counting leaves a partial cycle at each edge — see "A finding, not a fix,
  for peak_seed" above. The endpoint-completion mechanism is implemented
  and unit-tested to work when a genuine edge peak exists, but that's not
  what causes the shortfall in any of this suite's scenarios; the actual
  cause (peak phase generally offset from the analysis's arbitrary "true"
  phase reference) is architectural, not a bug, and isn't addressed by this
  fix. Flagging as the clearest remaining weakness, per the task's
  instructions to report rather than solve a newly-surfaced issue.
- **geometric_score**: the exact-integer-period-multiple ambiguity is
  handled for cases where the true and multiple-period candidates are
  near-tied in score; a case where a wrong-period candidate scores
  *unambiguously* higher (not just within `score_tolerance`) would not be
  caught by this fix and hasn't been demonstrated or ruled out.
  `candidate_score` remains unused in selection.
- **phase_ground_truth**: unchanged — not a real "failure," its fixed
  sample-0 anchoring convention just makes it unsuitable as ground truth
  for offset-recovery questions specifically.

## Whether the deterministic epoch-identification layer is ready to seed the Bayesian model

**Closer, but still no** — both demonstrated failures are fixed and
verified with no regressions, which removes the two clearest reasons not
to proceed. What's not yet established:

- The coverage-tie-break fix is validated on near-tied-score ambiguities
  (exactly the case that was demonstrated). It has not been tested against
  a case where a wrong candidate wins by a clear, non-thin margin — that
  would indicate an actual scoring-rule problem, out of scope here, and
  isn't known to occur or not occur.
- `peak_seed`'s remaining edge-cycle loss is now understood precisely
  rather than hand-waved, but not fixed. If Layer 1 seeding depends on
  `peak_seed` specifically (not `geometric_score`), this remains a live
  concern for real recordings where a cycle boundary genuinely falls near
  the recording's start or end.
- This validation suite is small: one tilted circle, one noise level, one
  warp amplitude, one harmonic distortion. Both fixes are demonstrated to
  work on this suite, not shown to generalize beyond it.

## Recommended next step

Broaden the validation suite's noise levels, offsets, and harmonic
distortions (still no Bayesian modeling) to check whether the
`score_tolerance` coverage tie-break and the endpoint-completion rule hold
up outside the exact conditions that motivated them, before treating either
as settled. In parallel or after that: decide whether `peak_seed`'s
edge-cycle loss (the finding from this round, not the fix) needs to be
addressed before Bayesian Layer 1 seeding proceeds, since it's now the
best-understood remaining deterministic weakness. `candidate_score`
(spectral confidence, still unused in selection) remains a smaller,
separate follow-up.
